#ifndef _PROT_H_
#define _PROT_H_

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <string.h>

void *zalloc(size_t size);

char *is_within(const char *str, char ch);

/*_PROT_H_*/
#endif
